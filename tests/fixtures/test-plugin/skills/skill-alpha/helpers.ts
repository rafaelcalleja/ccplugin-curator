/**
 * Helper functions for skill-alpha
 */

export const helper = () => {
  return "Helper function for skill-alpha";
};

export const formatData = (data: any) => {
  return JSON.stringify(data, null, 2);
};
